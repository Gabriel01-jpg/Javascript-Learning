// defineProperty - defineProperties
// Definir propriedades;

class Produto {
    /* this.nome = nome;
    this.preco = preco; */
    #x = 0;

    /* Object.defineProperty(this, 'estoque', {
        // enumerable - define se pode ser exibido, mostra a chave
        enumerable: true,
        // value define qual será o valor da propriedade;
        value: estoque,
        // writable - eu posso alterar o valor dessa propriedade?
        writable: true,
        // configurable - reconfigurar a chave ou apagar?
        configurable: true,

    }) */
}

const produto1 = new Produto();